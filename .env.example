# Copy to .env and fill in. Never commit .env.
# Generate values with: python3 -c "import secrets; print(secrets.token_urlsafe(24))"

# Gates the guide URL: https://<host>/<GUIDE_TOKEN>/epg.xml.gz
GUIDE_TOKEN=

# Gates /status and /refresh. Required when behind a reverse proxy.
ADMIN_TOKEN=
